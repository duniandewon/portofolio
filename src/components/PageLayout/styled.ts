import { styled } from "styled-components";

export const Container = styled.div`
  @media (min-width: 1024px) {
    display: flex;
    align-items: start;
    height: 100%;
    overflow: hidden;
  }
`;

import { styled } from "styled-components";

export const Container = styled.div`
  height: 100%;

  @media (min-width: 1024px) {
    display: flex;
    align-items: start;
    overflow: hidden;
  }
`;

import { styled } from "styled-components";

export const Container = styled.main`
  list-style: none;
  max-width: 95%;
  margin-inline: auto;
  padding-block: 2rem;
  padding-inline: 0;
  columns: 6 23rem;
  column-gap: 1rem;
`;

import Modal, { Props as ModalProps } from "../Modal";

interface Props extends ModalProps {
  image: string;
  title: string;
}

const ProjectDetail = () => {
  return <Modal>ProjectDetail</Modal>;
};

export default ProjectDetail;

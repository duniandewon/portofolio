import styled from "styled-components";

const Container = styled.nav`
  position: fixed;
  background: #fff;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  border-left: 5px solid #041c32;
`;

export default Container;
